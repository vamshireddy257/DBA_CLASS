To produce the issue create directory only in primary server.
only on primary primary:

mkdir -p /u02/datafiles
chmod -R 755 /u02/
chown -R oracle:oinstall /u02/



add a new datafile in above location.

open alert log in both location.


###unnamed datafile issue in standby ###

--on standby 
select FILE#||','||ERROR||','||STATUS||','||RECOVER||','||FUZZY||','||NAME from v$datafile_header where ERROR='FILE MISSING';
select file#,name from v$datafile where file#=15;


-- on primary
select FILE_NAME||','||TABLESPACE_NAME||','||FILE_ID||','||STATUS||','||bytes from dba_data_files where file_id = 15;


--on standby 
alter system set standby_file_management=MANUAL sid='*';

mkdir -p /u02/datafiles
chmod -R 755 /u02/
chown -R oracle:oinstall /u02/

alter database create datafile '/u01/app/oracle/product/19.0.0.0/dbhome_1/dbs/UNNAMED00015' as '/u02/datafiles/users_003.dbf' size 100M;

alter system set standby_file_management=AUTO sid='*';

alter database recover managed standby database disconnect from session;

alter system switch logfile;--on primary

set lines 300 pages 300;
SELECT sequence#, first_time, next_time, applied FROM v$archived_log ORDER BY first_time,sequence#;
